"use client";

/*
 * Disabled after mobile QA.
 * The clone-based cleaner assignment collapse broke the natural card layout/interactivity.
 * Keep this rollback-safe placeholder mounted; implement a future version inside the real React component instead.
 */
export default function AdminBookingCardsPolish() {
  return null;
}
